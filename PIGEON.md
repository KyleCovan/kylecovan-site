# Pigeon ornithopter (Level B)

**Bookmark for desktop.** This is the locked hardware/concept project — not a page on kylecovan.com.

## Open this

```
docs/concepts/pigeon/
```

Especially:

1. `docs/concepts/pigeon/LOCKED.md` — what “done” means  
2. `docs/concepts/pigeon/level-b-build.md` — build order  
3. `docs/concepts/pigeon/decision-gates.md` — gates  

## One-line summary

Rock-pigeon-scale anatomically inspired flapping flyer (~300–500 g), outdoor distance, camera aboard, **radio handset** as primary remote (iPhone = viewer only). Anatomical accuracy **Level B**.

## Branch / PR

Work lives on git branch `cursor/pigeon-ornithopter-5b92` (PR on that branch). Pull `main` only after merge if you want it on the default branch.

## Local path tip

On the desktop clone (`~/Projects/kylecovan-astro` or wherever this repo sits):

```bash
cd docs/concepts/pigeon && ls
```

Or open `PIGEON.md` at the repo root (this file).
